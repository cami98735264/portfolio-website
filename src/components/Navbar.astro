---
import User from "../icons/User.astro"
import Tools from "../icons/Tools.astro"
import Lightbulb from "../icons/Lightbulb.astro"
import Briefcase from "../icons/Briefcase.astro"
import { getLangFromUrl, useTranslations } from '../i18n/utils';

const lang = getLangFromUrl(Astro.url);
const t = useTranslations(lang);

---

<nav class="navbar">
  <div class="navbar__container">
    <a href={`/${lang}/`} class="navbar__link">
    <span class="navbar__link__icon">
        <User />
    </span>
    <span class="navbar__link__desc">
        <strong>{t('nav.aboutMe')}</strong>
    </span>
    </a>
    <a href={`/${lang}/projects`} class="navbar__link">
    <span class="navbar__link__icon">
        <Tools />
    </span>
    <span class="navbar__link__desc">
        <strong>{t('nav.projects')}</strong>
    </span>
    </a>
    <a href={`/${lang}/skills`} class="navbar__link">
    <span class="navbar__link__icon">
        <Lightbulb />
    </span>
    <span class="navbar__link__desc">
        <strong>{t('nav.skills')}</strong>
    </span>
    </a>
    <a href={`/${lang}/experience`} class="navbar__link">
    <span class="navbar__link__icon">
        <Briefcase />
    </span>
    <span class="navbar__link__desc">
        <strong>{t('nav.experience')}</strong>
    </span>
    </a>

</div>
</nav>


<style>
    .navbar__container {
        height: 100px;
        display: flex;
        justify-content: space-around;
        align-items: center;
        background-color: #161616;
    }
    .navbar {
        position: fixed;
        bottom: 0;
        width: 100%;
    }
    .navbar__link__desc strong {
        text-align: center;
        font-size: clamp(0.6rem, 2vw, 0.9rem);

    }
    .navbar__link {
        flex: 1 1 0;
    }
    .navbar__container a {
        display: flex;
        gap: 5px;
        flex-direction: column;
        align-items: center;
        justify-content: center;
        color: #6b6b6b;
        padding: 16px;
        text-decoration: none;
    }

    @media (width < 575px) {
        .navbar__link__icon * {
            width: 30px;
            height: 30px;
        }
    }
</style>